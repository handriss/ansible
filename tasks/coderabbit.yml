- name: Install CodeRabbit CLI
  ansible.builtin.shell: |
    set -euo pipefail
    curl -fsSL https://cli.coderabbit.ai/install.sh | bash
  args:
    executable: /bin/bash
    creates: "{{ lookup('env', 'HOME') }}/.local/bin/coderabbit"
